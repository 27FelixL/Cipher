"""
Student information for this assignment:

On my/our honor, Felix Li, this
programming assignment is my own work and I have not provided this code to
any other student.

I have read and understand the course syllabus's guidelines regarding Academic
Integrity. I understand that if I violate the Academic Integrity policy (e.g.
copy code from someone else, have the code generated by an LLM, or give my
code to someone else), the case shall be submitted to the Office of the Dean of
Students. Academic penalties up to and including an F in the course are likely.

UT EID 1: fl7449
"""

def rail_fence_encode(string, key):
    """
    pre: string is a string of characters and key is a positive
        integer 2 or greater and strictly less than the length
        of string
    post: returns a single string that is encoded with
        rail fence algorithm
    """
    rails = [''] * key
    row = 0
    direction = 1
    for ch in string:
        rails[row] += ch
        row += direction
        if row == 0 or row == key - 1:
            direction *= -1
    return ''.join(rails)


def rail_fence_decode(string, key):
    """
    pre: string is a string of characters and key is a positive
        integer 2 or greater and strictly less than the length
        of string
    post: function returns a single string that is decoded with
        rail fence algorithm
    """
    n = len(string)
    mark = [[False] * n for _ in range(key)]
    row = 0
    direction = 1
    for col in range(n):
        mark[row][col] = True
        row += direction
        if row == 0 or row == key - 1:
            direction *= -1
    decoded = [[None] * n for _ in range(key)]
    idx = 0
    for r in range(key):
        for c in range(n):
            if mark[r][c]:
                decoded[r][c] = string[idx]
                idx += 1
    result = []
    row = 0
    direction = 1
    for col in range(n):
        result.append(decoded[row][col])
        row += direction
        if row == 0 or row == key - 1:
            direction *= -1
    return ''.join(result)

def filter_string(string):
    """
    pre: string is a string of characters
    post: function converts all characters to lower case and then
        removes all digits, punctuation marks, and spaces. It
        returns a single string with only lower case characters
    """
    s = string.lower()
    return ''.join(ch for ch in s if 'a' <= ch <= 'z')


def encode_character(p, s):
    """
    pre: p is a character in the pass phrase and s is a character
        in the plain text
    post: function returns a single character encoded using the
        Vigenere algorithm. You may not use a 2-D list
    """
    return chr((ord(s) - ord('a') + ord(p) - ord('a')) % 26 + ord('a'))


def decode_character(p, s):
    """
    pre: p is a character in the pass phrase and s is a character
        in the encrypted text
    post: function returns a single character decoded using the
        Vigenere algorithm. You may not use a 2-D list
    """
    return chr((ord(s) - ord('a') - (ord(p) - ord('a')) + 26) % 26 + ord('a'))

def vigenere_encode(string, phrase):
    """
    pre: string is a string of characters and phrase is a pass phrase
    post: function returns a single string that is encoded with
        Vigenere algorithm
    """
    filtered = filter_string(string)
    result = []
    m = len(filtered)
    if m == 0:
        return ''
    plen = len(phrase)
    phrase_l = phrase.lower()
    j = 0
    for i in range(m):
        p = phrase_l[j % plen]
        result.append(encode_character(p, filtered[i]))
        j += 1
    return ''.join(result)

def vigenere_decode(string, phrase):
    """
    pre: string is a string of characters and phrase is a pass phrase
    post: function returns a single string that is decoded with
        Vigenere algorithm
    """
    filtered = filter_string(string)
    result = []
    m = len(filtered)
    if m == 0:
        return ''
    plen = len(phrase)
    phrase_l = phrase.lower()
    j = 0
    for i in range(m):
        p = phrase_l[j % plen]
        result.append(decode_character(p, filtered[i]))
        j += 1
    return ''.join(result)


def main():
    """Main function that reads stdin and runs each cipher"""
    print("Rail Fence Cipher")
    print()
    plain_rf = input()
    key_rf = int(input())
    encoded_rf = rail_fence_encode(plain_rf, key_rf)
    print(f"Plain Text: {plain_rf}")
    print(f"Key: {key_rf}")
    print(f"Encoded Text: {encoded_rf}")
    print()
    cipher_rf = input()
    key_rf2 = int(input())
    decoded_rf = rail_fence_decode(cipher_rf, key_rf2)
    print(f"Encoded Text: {cipher_rf}")
    print(f"Enter Key: {key_rf2}")
    print(f"Decoded Text: {decoded_rf}")
    print()

    print("Vigenere Cipher")
    print()
    plain_vig = input()
    phrase_vig = input()
    encoded_vig = vigenere_encode(plain_vig, phrase_vig)
    print(f"Plain Text: {plain_vig}")
    print(f"Pass Phrase: {phrase_vig}")
    print(f"Encoded Text: {encoded_vig}")
    print()
    cipher_vig = input()
    phrase_vig2 = input()
    decoded_vig = vigenere_decode(cipher_vig, phrase_vig2)
    print(f"Encoded Text: {cipher_vig}")
    print(f"Pass Phrase: {phrase_vig2}")
    print(f"Decoded Text: {decoded_vig}")

if __name__ == "__main__":
    main()
